# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema
$schema: https://json-schema.org/draft/2020-12/schema
$id:     <HOST>/<PREFIX>/<VERSION>/outputs/resource/schema.yaml

title: DSC resource schema result
description: >-
  Describes the return data for a DSC resource from the `dsc resource schema`
  command. This command always returns the DSC resource's JSON schema document.

type: object
