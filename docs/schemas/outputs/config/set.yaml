# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema
$schema: https://json-schema.org/draft/2020-12/schema
$id: https://schemas.microsoft.com/dsc/2023/07/results/config/set.yaml

title: DSC Configuration set command result
description: >-
  Represents the data structure returned by the `dsc config set` command.

type: object
required:
  - results
  - messages
  - hadErrors
properties:
  results:
    title: Results
    description: >-
      The results of the `set` method for every DSC Resource instance in the
      DSC Configuration Document with the instance's name and type.
    type: array
    items:
      title: Set Result
      type: object
      required:
        - name
        - type
        - result
      properties:
        name:
          $ref: /dsc/2023/07/definitions/instanceName.yaml
        type:
          $ref: /dsc/2023/07/definitions/resourceType.yaml
        result:
          $ref: /dsc/2023/07/results/resource/set.yaml
  messages:
    $ref: /dsc/2023/07/definitions/messages.yaml
  hadErrors:
    $ref: /dsc/2023/07/definitions/hadErrors.yaml
